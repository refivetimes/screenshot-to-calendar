GEMINI_API_KEY=your_api_key_here
# Optional: override model selection. If unset, the server auto-tries modern models.
# GEMINI_MODEL=gemini-2.5-flash
# Optional: default Apple Calendar name when input does not specify one.
# DEFAULT_CALENDAR=Home
